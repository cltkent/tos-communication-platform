/**
 * Data Transfer Objects (DTOs).
 *
 * <p>DTOs cross layer boundaries (consumer → core, core → adapter, controller → core)
 * and are annotated with Lombok {@code @Data} / {@code @Builder} for brevity.
 * They must NOT carry JPA or persistence annotations.
 */
package com.clt.toscana.domain.dto;

/**
 * Spring Data JPA repositories.
 *
 * <p>Only the {@code core/service} layer and above may call repository methods.
 * Repositories must NOT be referenced from {@code adapter} or {@code consumer}.
 */
package com.clt.toscana.repository;

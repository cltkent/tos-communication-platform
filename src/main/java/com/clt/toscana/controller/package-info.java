/**
 * REST API controllers (WebFlux {@code @RestController}).
 *
 * <p>Sub-packages:
 * <ul>
 *   <li>{@code admin} – internal management endpoints (send blast, view logs, …)</li>
 *   <li>{@code user}  – user-facing endpoints (subscribe, preferences, …)</li>
 * </ul>
 */
package com.clt.toscana.controller;
